[project]
name = "todo-app"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12.12"
dependencies = [
    "fastapi>=0.128.3",
    "fastmcp>=2.14.5",
    "jinja2>=3.1.6",
    "python-multipart>=0.0.22",
    "uvicorn[standard]>=0.40.0",
    "kriy-agentic",
]

# Use the local SDK from this monorepo (zero-dependency, stdlib-only).
[tool.uv.sources]
kriy-agentic = { path = "../../sdk/python", editable = true }
