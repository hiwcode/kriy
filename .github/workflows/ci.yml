name: CI

# Runs on every push to main and on PRs targeting main, so breakage is caught
# automatically instead of manually re-testing each time.
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  backend:
    name: Backend (pytest + boot)
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: kriy_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    env:
      # Throwaway Postgres — not secret. The signing secrets are GENERATED per run
      # below (never committed) so no key-looking value lives in the repo.
      # 127.0.0.1 (not localhost) to avoid IPv6 ::1 resolution vs the IPv4 port map.
      DATABASE_URL: postgresql://postgres:postgres@127.0.0.1:5432/kriy_test
      ENVIRONMENT: development
    steps:
      - uses: actions/checkout@v4
      - name: Generate throwaway CI secrets
        run: |
          # A valid Fernet key = urlsafe-base64 of 32 random bytes. Ephemeral to this run.
          echo "ENCRYPTION_KEY=$(openssl rand -base64 32 | tr '+/' '-_')" >> "$GITHUB_ENV"
          echo "JWT_SECRET=$(openssl rand -hex 24)" >> "$GITHUB_ENV"
      - name: Install uv
        uses: astral-sh/setup-uv@v5
        with:
          python-version: "3.12"
      - name: Install dependencies (incl. dev)
        run: uv sync --dev
      - name: Run tests
        run: uv run pytest -q
      - name: Validate integration documentation
        run: uv run python scripts/check_docs.py

  frontend:
    name: Frontend (typecheck + lint + build)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: web
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: web/package-lock.json
      - name: Install dependencies
        run: npm ci
      # Typecheck catches type/import breakage.
      - name: Typecheck
        run: npm run typecheck
      # Blocking: the tree is lint-clean (0 errors). ESLint still exits 0 on
      # warnings, so remaining non-error debt is surfaced without failing CI.
      - name: Lint
        run: npm run lint
